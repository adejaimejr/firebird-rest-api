# ============================================================================
# CONFIGURAÇÃO DO GATEWAY FIREBIRD API
# ============================================================================
# Copie este arquivo para .env e preencha os valores
#
# ⚠️  VARIÁVEIS OBRIGATÓRIAS (servidor NÃO inicia sem elas):
#    - API_KEY
#    - FB_HOST
#    - FB_DATABASE
#    - FB_USER
#    - FB_PASSWORD

# ----------------------------------------------------------------------------
# SERVIDOR NODE.JS
# ----------------------------------------------------------------------------
NODE_ENV=production
PORT=3030

# ----------------------------------------------------------------------------
# DOMÍNIO (para Traefik SSL)
# ----------------------------------------------------------------------------
# Domínio público da API (usado nas labels do Traefik)
# Exemplo: api.meudominio.com.br
DOMAIN=

# ----------------------------------------------------------------------------
# CONEXÃO FIREBIRD (⚠️ OBRIGATÓRIAS)
# ----------------------------------------------------------------------------
# IP ou hostname do servidor Firebird
FB_HOST=

# Porta do Firebird (padrão: 3050)
FB_PORT=3050

# Caminho completo do banco de dados no servidor Firebird
FB_DATABASE=

# Usuário do banco
FB_USER=

# Senha do banco
FB_PASSWORD=

# ----------------------------------------------------------------------------
# POOL DE CONEXÕES
# ----------------------------------------------------------------------------
# Número mínimo de conexões mantidas abertas
POOL_MIN=2

# Número máximo de conexões simultâneas
POOL_MAX=10

# Tempo em ms para fechar conexões ociosas (30 segundos)
POOL_IDLE_TIMEOUT=30000

# Intervalo em ms para verificar conexões ociosas (5 segundos)
POOL_CHECK_INTERVAL=5000

# ----------------------------------------------------------------------------
# SEGURANÇA (⚠️ OBRIGATÓRIA)
# ----------------------------------------------------------------------------
# API Key para autenticação - GERE UMA CHAVE FORTE!
# Como gerar: openssl rand -hex 32
API_KEY=

# Bloquear operações DDL destrutivas (DROP, TRUNCATE, ALTER, CREATE)
# Valores: true | false
BLOCK_DDL=true

# Permitir operações de escrita (UPDATE, INSERT, DELETE)
# Valores: true | false
# ATENÇÃO: Mude para 'true' apenas se necessário
ALLOW_WRITE_OPS=true

# ----------------------------------------------------------------------------
# RATE LIMITING
# ----------------------------------------------------------------------------
# Janela de tempo em ms (60000 = 1 minuto)
RATE_LIMIT_WINDOW=60000

# Máximo de requisições por janela de tempo
RATE_LIMIT_MAX=100
